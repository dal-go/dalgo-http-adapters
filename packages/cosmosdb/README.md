# DALgo adapter for Azure Cosmos DB for NoSQL

`@dal-go/dalgo2cosmosdb` is a deliberately bounded adapter from
[`@dal-go/dalgo`](https://github.com/dal-go/dalgo-js) to the Azure Cosmos DB
for NoSQL HTTPS data-plane API. It owns one configured, existing container;
it does not create databases or containers.

## Security and browser boundary

This is **HTTP-capable / browser with an ephemeral token**, not browser-native
authentication. The adapter accepts a per-request `authorization` provider and
does not accept, store, log, or sign with a Cosmos account master key.

For an untrusted browser, a trusted backend must authenticate the user and
issue a short-lived, least-privilege Cosmos resource token for the relevant
resource and permission. The browser passes that opaque token through the
provider. Do not put master keys, resource tokens, Entra bearer tokens, or
authorization values in URLs, source control, analytics, errors, browser
storage, or logs. The backend may alternatively use a trusted proxy or an
appropriately designed Entra/network boundary; validating that deployment,
CORS policy, token audience, expiry, and resource scope is application work,
not something this library can infer.

Microsoft documents that resource tokens are generated by an intermediate
server for untrusted clients, while master-key tokens provide account-wide
control. See [Cosmos REST access control](https://learn.microsoft.com/rest/api/cosmos-db/access-control-on-cosmosdb-resources)
and the [REST API reference](https://learn.microsoft.com/rest/api/cosmos-db/).

## Install

This package is developed in the repository workspace and is not yet published
to npm. From the repository root, run `pnpm install --frozen-lockfile` and
`pnpm --filter @dal-go/dalgo2cosmosdb build`.

## Required container layout

Create the container yourself with a partition key matching
`/__dalgo_partition` (or the configured `partitionKeyField`). The adapter maps
one DALgo collection path to exactly one physical Cosmos partition. By default
that partition value is the collection path. A custom
`partitionKey(collectionPath)` may be used only when all records in a DALgo
collection resolve to the same scalar partition value.

| Cosmos property | Value |
| --- | --- |
| `id` | A deterministic base64url encoding of the DALgo key ID. |
| `__dalgo_collection` | `key.collectionPath`, including any parent path. |
| `__dalgo_id` | Original DALgo string or numeric key ID. |
| `__dalgo_partition` | Configured partition mapping value. |
| `data` | Codec-encoded DALgo document object. |

The configured physical partition-key property must exist in the container
definition and must not be changed after data is written. The adapter always
sends `x-ms-documentdb-partitionkey`; it never guesses a partition key or runs
a cross-partition query.

## Minimal setup

```ts
import { collection } from "@dal-go/dalgo";
import { CosmosNoSqlDatabase } from "@dal-go/dalgo2cosmosdb";

const db = new CosmosNoSqlDatabase({
  endpoint: "https://example.documents.azure.com",
  databaseId: "app",
  containerId: "records",
  authorization: async () => (await fetch("/api/cosmos-resource-token")).text(),
});

const items = collection<{ title: string; done: boolean }>("items");
await db.insert(items.key("milk"), { title: "Buy milk", done: false });
```

The token broker must be authenticated and trusted. Do not persist its token
in localStorage or a URL. The complete type-checked example is
[examples/basic.ts](examples/basic.ts).

## Supported DALgo surface

| DALgo operation | Cosmos mapping |
| --- | --- |
| `get` / `getMany` | Point `GET` requests with explicit partition headers; input order is preserved. |
| `insert` | `POST /docs`; HTTP 409 maps to `AlreadyExistsError`. |
| `set` | Document `POST /docs` with the documented `x-ms-documentdb-is-upsert: True` header; preserves DALgo create-or-replace semantics. |
| `delete` | `DELETE /docs/{id}`; a missing document is idempotently accepted. |
| `query` | Parameterized Cosmos SQL `POST /docs`, restricted to one DALgo collection and partition. |
| pagination | The service's opaque `x-ms-continuation` becomes an adapter-bound DALgo `startAfter` cursor. |

Query requests use the documented `application/query+json`,
`x-ms-documentdb-isquery`, partition-key, max-item-count, and continuation
headers. Cosmos SQL parameter values are sent separately; user values and
field names are not interpolated as SQL values. See [REST querying](https://learn.microsoft.com/rest/api/cosmos-db/querying-cosmosdb-resources-using-the-rest-api)
and [query pagination](https://learn.microsoft.com/azure/cosmos-db/nosql/query/pagination).

## Intentional limitations

- `update` is rejected. A transparent DALgo partial-update mapping would need
  Cosmos Patch semantics and a clear atomic/precondition contract; silently
  doing a read-modify-write loses concurrency safety.
- DALgo callback transactions are rejected. Cosmos transactional batches are
  partition-scoped, but cannot preserve DALgo callback/retry semantics.
- Collection-group queries, offsets, document-ID or field ordering, inclusive
  cursors, end cursors, unsupported filters, hand-built cursors, aggregation,
  and change-feed/realtime are not implemented.
- `limit` is sent as `x-ms-max-item-count`; it defaults to 100 and is bounded
  by `maxQueryLimit` (at most 1000). Cosmos can return fewer results and its
  next opaque cursor must be used to continue. `getMany` is likewise bounded
  (`maxGetManyKeys`, default 100) and uses bounded parallel reads.
- This package does not configure Azure CORS, private endpoints, firewall
  rules, Entra identity, a token broker, container indexing, throughput, or
  retry/throttling. Requests default to a 15-second timeout and 1 MiB request
  and response bounds (configurable). Oversized response streams are cancelled.
  HTTP error bodies are deliberately redacted; `CosmosHttpError` exposes only
  status and a validated `retryAfterMs` derived from `x-ms-retry-after-ms`.

## Verification

`pnpm check` runs ESLint, deterministic Vitest HTTP-contract tests, the
declaration build, and a type check of the minimal example. Tests inject a
mock fetch implementation; they do not contact a Cosmos account and are not
live-service acceptance coverage.

## License

MIT
